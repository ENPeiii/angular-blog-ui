name: angular-blog
services:
  ui:
    image: ghcr.io/enpeiii/angular-blog-ui:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:4000:4000"
    environment:
      PORT: 4000
      NODE_ENV: production

