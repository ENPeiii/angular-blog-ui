services:
  angular-blog-ui:
    image: ghcr.io/enpeiii/angular-blog-ui:latest
    container_name: angular_blog_ui
    restart: unless-stopped
    ports:
      - "127.0.0.1:4000:4000"
    environment:
      PORT: 4000
      NODE_ENV: production
