/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseTag } from '../../models/api-response-tag';

export interface GetTag_1$Params {

/**
 * 標籤 ID
 */
  id: string;
}

export function getTag_1(http: HttpClient, rootUrl: string, params: GetTag_1$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseTag>> {
  const rb = new RequestBuilder(rootUrl, getTag_1.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseTag>;
    })
  );
}

getTag_1.PATH = '/admin/tags/{id}';
